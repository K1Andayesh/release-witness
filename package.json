{
  "name": "release-witness",
  "version": "0.1.10",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "witness": "node --env-file-if-exists=.env scripts/witness-cli.mjs",
    "record:demo": "node scripts/record-demo.mjs",
    "check": "node --check server.mjs && node --check lib/runner.mjs && node --check lib/manifests.mjs && node --check lib/cli.mjs && node --check scripts/witness-cli.mjs && node --check scripts/record-demo.mjs && node --check public/app.js && node --check public/booking.js",
    "test": "node --test tests/*.test.mjs",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "playwright": "1.63.0"
  },
  "devDependencies": {
    "prettier": "3.9.6"
  }
}
