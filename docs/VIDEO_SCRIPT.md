# Demo video script — two-workflow natural narration cut: 1:47

## 0:00–0:10 — Problem and product

Show the public appointment comparison and judge entry point.

“A release result needs a receipt. Release Witness turns a developer's change into bounded risks, runs reviewed browser checks, and preserves evidence a human can assess.”

## 0:10–0:24 — Bounded model role

Show the saved live Nemotron risk map.

“NVIDIA Nemotron 3.5 Lightning, served through Nebius Token Factory, maps the change to every reviewed check before Chrome runs. It may prioritize the complete catalog, but it cannot remove coverage or decide a verdict.”

## 0:24–0:53 — Appointment baseline and candidate

Show the baseline persistence failure, then the candidate's expanded before-and-after comparison.

“This appointment baseline contains two seeded defects: a booking disappears after reload, and a sold-out time remains selectable. Release Witness detects both while preserving the passing name-validation invariant.

The candidate resolves both concerns with zero observed regressions. Each changed check opens the before-and-after observations, reproduction steps, and screenshots together. Authentication, payments, and other browsers remain explicitly unverified.”

## 0:53–1:09 — Independent workflow proof

Show the Fieldnotes persistence repair with its paired evidence.

“The same contract runs against an independent Fieldnotes workflow. Its persistence defect changes from fail to pass, while blank-note validation and duplicate prevention stay passing. This shows reuse of the model contract and browser runner without scenario-specific code.”

## 1:09–1:27 — Portfolio measurement

Return to the judge strip and portfolio-wide Nemotron proof.

“Across both workflows, Release Witness detects three of three known defects and classifies three of three repairs, with zero observed regressions. Four receipt-verified Nemotron runs contribute twelve grounded risk hypotheses and four allow-listed advisories. Browser assertions still own every verdict.”

## 1:27–1:43 — Source-bound receipt

Show the benchmark receipt, source links and full hashes.

“The portfolio receipt freshly verifies four run receipts and sixteen screenshot files. Its aggregate digest binds the exact release, immutable Git commit, and downloadable source-archive hash, so a judge can trace the result back to the code that produced it.”

## 1:43–1:47 — Close

“Release Witness. Evidence before confidence.”

## Recording checklist

- Resolve the current public pair IDs from `/api/benchmark`; do not hard-code historical runs.
- Record at 1600 × 900 with browser zoom at 100% and hide notifications and secrets.
- Use saved reports so the take is repeatable and consumes no model calls.
- Use a natural Australian English neural voice and inspect the final H.264/AAC cut.
- Verify the final cut is under three minutes and that repository, demo and video links work in a signed-out browser.
